---
- name: Node  ----  Modules AI  ----  Install Node.js applications
  community.general.npm:
    name: "{{ item }}"
    global: true
    state: present
  loop:
    - "@google/gemini-cli"
    - "@github/copilot"
