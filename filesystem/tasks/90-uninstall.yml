---
- name: Filesystem  ----  Uninstall  ----  Remove directories
  ansible.builtin.file:
    path: "{{ item }}"
    state: absent
  with_items:
    - /home/{{ default_user }}/.config/libreoffice
    - /home/{{ default_user }}/.repos
