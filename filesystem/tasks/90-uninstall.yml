---
- name: Filesystem  ----  Uninstall  ----  Remove directories
  ansible.builtin.file:
    path: "{{ item }}"
    state: absent
  with_items:
    - /home/{{ ansible_user }}/.config/libreoffice
    - /home/{{ ansible_user }}/.repos
